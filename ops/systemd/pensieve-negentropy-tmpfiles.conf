# Install explicitly before enabling the worker. The setgid directory makes
# the ingester-created 0660 socket inherit pensieve-ipc, without changing the
# ingester's primary group or granting the worker archive/data access.
d /run/pensieve 2770 pensieve pensieve-ipc - -
